use bevy::prelude::*;

// TASK: Move to `graphics::nav_marker`
pub struct NavMarker {
    // TASK: Rename to `entity`.
    // TASK: Make private, once this lives in `graphics::nav_marker`.
    pub nav_marker: Entity,
}
